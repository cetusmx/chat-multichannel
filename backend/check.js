const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const config = await prisma.aiConfig.findMany(); console.log(config); } main().finally(() => prisma.\$disconnect());
